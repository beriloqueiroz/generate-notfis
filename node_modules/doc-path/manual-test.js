const path = require('./lib/path.js');

// let document = {};

// console.log({}.polluted)
// path.setPath(document,"a.__proto__.polluted","Yep!")
// console.log({}.polluted)
// // console.log(polluted)
// console.log(Object.polluted);

// assert.equal(doc.polluted, undefined);
// path.setPath(doc, 'a.__proto__.polluted', 'polluted!');
// console.log(doc.polluted);
// console.log({}.polluted);
// console.log(Object.polluted);

let document = {};
path.setPath(document, 'a.__proto__.pollution', 'polluted!');
console.log(document.pollution);
